server:
  port: 8080
  address: 0.0.0.0
  servlet:
    context-path: /fapanese

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fapanese_database
    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: fapanese.edu@gmail.com
    password: ggqttilytuikozmy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          debug: true

JWT_SIGNER_KEY: "Av8kEwq3M2Yh5Rt9Xn1Zp7Lm4Jc6Fd0Sg2Uw8Bv5Nq3"
jwt:
  signerKey: ${JWT_SIGNER_KEY}
  valid-duration:  3600 # in second
  refreshable-duration: 36000 # in seconds

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME}
  api-key: ${CLOUDINARY_API_KEY}
  api-secret: ${CLOUDINARY_API_SECRET}


logging:
  level:
    org.springframework.security: DEBUG

